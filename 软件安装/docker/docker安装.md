# docker安装

1. 脚本

   ``` shell
    curl -fsSL get.docker.com -o get-docker.sh |sh
    bash 
   ```

   

2. 启动

   ``` shell
   
   sudo systemctl enable docker
   sudo systemctl start docker
   ```

   

3. 